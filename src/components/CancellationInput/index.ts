import CancellationInput from './src/CancellationInput.vue'

/**
 * CancellationPolicyInput
 * @description: 此元件綁定一個cancellation policy陣列，並根據陣列內容動態生成對應的輸入框並回反饋輸入值到綁定的陣列中
 */
export { CancellationInput }
